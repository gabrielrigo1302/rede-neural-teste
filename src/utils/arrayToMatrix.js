ArrayToMatrix = (arr) => {

//    let matrix = new Matrix(arr.length, 1);
//    matrix.map((elm, i, j) => {
//        return arr[i];
//    })
//    return matrix; 

}

module.export = {
    ArrayToMatrix
}